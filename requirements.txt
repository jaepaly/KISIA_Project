# =============================================================
#  KISIA 3팀(파도풀) — Python 의존성
#
#  ⚠️ Python 3.11 필수.  scikit-learn 1.9.0 이 >=3.11 을 요구한다.
#     3.10 에서는 pip 가 전부 롤백해 아무것도 안 깔린다 (실측 2026-08-24).
#
#  ⚠️ torch 는 이 파일로 깔지 않는다. GPU 마다 CUDA 빌드가 달라야 하므로
#     아래 「설치」 절차대로 별도 인덱스에서 먼저 깐다.
#
#  설치 절차와 역할별 최소 구성은 docs/setup-python.md 참조.
# =============================================================

# --- 공통 -----------------------------------------------------
transformers==5.15.1
accelerate==1.14.0
datasets==5.0.1
scikit-learn==1.9.0

# --- 2단 QLoRA (D) --------------------------------------------
#  A·C·E 는 없어도 되지만, 버전이 갈리면 재현이 깨지므로 같이 고정한다.
peft==0.20.0
bitsandbytes==0.50.1

# --- torch (별도 설치, 버전만 여기 기록) ------------------------
#  pip install torch==2.11.0+cu128 --index-url https://download.pytorch.org/whl/cu128
#
#  cu128 로 고정한 이유
#    · B 최진필 RTX 5070 은 Blackwell(sm_120) 이라 CUDA 12.8 미만에서 아예 안 돈다
#    · D 박재현 RTX 3060(sm_86) 에서 12.8 정상 동작 확인
#    → 두 GPU 를 모두 커버하는 유일한 하한선이다
#  CPU 만 쓰는 경우: --index-url https://download.pytorch.org/whl/cpu
#
# torch==2.11.0+cu128
