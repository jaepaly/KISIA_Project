# 모델 레지스트리

> **가중치는 여기에 커밋하지 않는다.** 포인터만 남긴다.
>
> Git LFS도 쓰지 않는다 — GitHub 무료 LFS는 1GB 스토리지/1GB 월 대역폭이라
> 440MB 체크포인트 2~3개로 즉시 초과된다.

## 공개 정책 — 이중용도 통제

| 모델 | 성격 | 공개 |
|---|---|---|
| **1단 탐지기** | 방어 측 | private → **M4에 public** |
| **2단 추론기** | 공격 측(속성 추론) | **비공개 유지** |

탐지기를 잘 만들면 그게 곧 추론기다. 방어 측만 공개한다 ([plan.md §13.5](../docs/plan.md)).

## 등록

| 이름 | 단계 | 베이스 | 방식 | 위치 | 학습일 | 데이터 버전 | 지표 |
|---|---|---|---|---|---|---|---|
| | 1단 | KoELECTRA-base | full FT | | | | |
| | 2단 | **Qwen3-4B** ✅ 확정 2026-08-31 | QLoRA 4bit | | | | exp05 |

**모든 행에 학습일 · 데이터 버전 · 지표를 함께 적는다.** 없으면 10주 뒤에
그 체크포인트가 무엇이었는지 복원할 수 없다.

## 추론 요구사항 (제품 기준)

| 모델 | 4bit 추론 VRAM | 대상 |
|---|---|---|
| Qwen3-4B | ~3GB | 일반 노트북 |
| ~~Qwen3-8B~~ | ~6GB | **후보 제외** — 일반 노트북 기준을 벗어난다 |

**학습 VRAM과 추론 VRAM은 다른 문제다.** 학습 데이터가 전량 합성이라
학습은 클라우드에서 해도 되고, 진짜 제약은 **사용자 PC에서의 추론**이다.

## 로컬 동작 확인 — 2026-08-29 (D)

W2 완료 기준 「Qwen3-4B 가 로컬에서 돌아감」의 실측 기록이다.
**아직 학습 전이고 베이스 모델을 그대로 돌린 것이다.**

| | |
|---|---|
| 실행 | ollama (로컬 HTTP · 외부 전송 0) |
| 장비 | RTX 3060 8GB · torch 2.12.1+cu130 · transformers 5.12.1 |
| 받아둔 것 | `qwen3:4b` (2.5GB) · `qwen3:1.7b` (1.4GB) |
| 아직 없는 것 | `peft` · `bitsandbytes` — QLoRA 학습 시 필요 |

### 첫 스모크 테스트

D06 인물의 나이 단서 한 문장을 넣고 `{"age_band","evidence"}` JSON 을 요구했다.
`temperature=0`.

| 모델 | 모드 | 시간 | 출력 토큰 | 결과 |
|---|---|---|---|---|
| **4B** | 사고 허용 | 16.0s | 845 | `age_band: "30대"` · evidence 에 **본문 전체**를 인용 |
| 4B | `/no_think` | 6.3s | 200(상한) | 형식 이탈 — 상한 안에 답을 못 냄 |
| **1.7B** | 사고 허용 | **5.3s** | **204** | `age_band: "38"` · evidence **`"서른여덟."`** |
| 1.7B | `/no_think` | 9.5s | 200(상한) | JSON 시작하나 잘림 |

⚠️ **표본 1건이다. 크기 결정의 근거로 쓰기에는 부족하다.** 다만 두 가지는
지금 적어둘 값이 있다.

1. **`think:false` 옵션이 안 먹는다.** 프롬프트에 `/no_think` 를 붙여야 하고,
   그러면 이번 표본에서는 두 모델 다 형식을 못 지켰다. **사고 토큰을 빼는 것이
   공짜가 아니다** — 2단 프롬프트 설계에서 토큰 예산을 그만큼 잡아야 한다.
2. **1.7B 가 근거 인용을 더 좁게 했다.** 4B 는 본문 전체를 evidence 로 넣었고
   1.7B 는 `"서른여덟."` 한 어절만 넣었다. 우리 2단 출력은 **근거 스팬**을
   요구하므로 이 차이가 그대로 지표에 걸린다. 크기가 크다고 유리한 축이 아니다.

**W3 초 크기 결정(1.7B/4B)에서 이 두 가지를 같은 프롬프트로 n≥20 재측정한다.**


## 2단 베이스 크기 결정 — 2026-08-31 (D) ✅

**`qwen3:4b` 로 확정한다.** 근거는 `experiments/exp05-model-size/`.
W2 스모크(표본 1건)를 **n=35** 로 다시 쟀다.

| | `qwen3:1.7b` | `qwen3:4b` |
|---|---:|---:|
| ① 전체 245칸 정확도 | 91.0% | **93.5%** |
|  · 양성(단서 있음 → 발언) | 98.3% | 98.3% |
|  · 음성(단서 없음 → 기권) | 71.2% | **80.3%** |
| ② 의미상 근거 정확도 | 69.6% | **83.1%** |
|  · 함정을 근거로 쓴 인물 | 31/34 | **22/34** |
| ③ 형식 값 제약(confidence 0~1) | 34/35 | **35/35** |
| ④ 편당 토큰 / 지연 | **303 / 5.4s** | 309 / 7.9s |

**판정 우선순위 ①②③ 에서 전부 4B 가 앞선다.** ④는 동률일 때만 보는 축이라 적용되지 않았다.

**갈린 자리는 「말을 아끼는 능력」이다.** 단서가 있을 때 발언하는 것은 98.3% 로 완전 동률이고,
차이는 전부 **단서가 없을 때 기권하는가**와 **남의 정보를 근거로 쓰지 않는가**에서 나왔다.

### 조건

```
측정일 2026-08-31 · 데이터 corpus-v0 (인물 35명 · 스팬 276건)
ollama 0.33.2 · RTX 3060 8GB · temperature=0 · seed=20260901 · think=false
출력은 ollama format 으로 JSON 스키마 강제
```

**`think=false` 가 이번에는 먹었다.** W2 에 안 먹은 것은 `/no_think` 프롬프트 토큰 쪽이었고,
API 의 `think` 필드는 정상 동작한다. 그리고 `format` 강제로 **형식 붕괴가 사라졌다**
— W2 에 두 모델 다 형식을 못 지키던 문제가 구조 강제만으로 해결됐다(구조 35/35 · 양쪽).

### 한계 (W4 설계서에 같이 적는다)

- **prompt-only 스팬 판정에 한정된 선택이다.** 전체 `Stage2Output`·글 간 결합·k 값·QLoRA 후 성능은 재지 않았다
- **`D-stage2.md` §4 의 「7속성 중 5개 적중」을 입증한 것이 아니다.** 그 기준은 **값 적중**이고
  계약 `Finding` 에는 속성 값 필드가 없다. 이 측정의 ①은 **특정 가능성 판정**이다
- **선행 PoC 의 R1~R5 7/7 · R6 6/7 과 직접 비교 불가.** R1~R6 원 조건이 저장소에 없다
- n=35 · 단일 seed · 1회 실행. `sex` 는 표본이 5칸뿐이다

### ⚠️ 여기서 나온 W4 학습 1순위

**두 모델 다 함정을 근거로 쓴다** (4B 도 34명 중 22명). `subject: other` 스팬을 본인 속성의
근거로 쓰는 것은 계약이 금지한 동작이고, **기존 도구와 갈리는 지점이 정확히 여기다.**
프롬프트만으로는 안 된다 — QLoRA 학습의 1순위 목표로 둔다.
