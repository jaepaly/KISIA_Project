# 대회 작업 일지 — 원티드 AI Championship 2026

> `apps/demo/` 에서 한 작업·발견·결정을 날짜별로 남긴다. **→ 본 프로젝트** 표시는 KISIA 본 프로젝트(B·C·D·E 파트)에
> 그대로 해당하는 발견이다 — 데모 브랜치는 로컬 전용이라 여기 안 적으면 사라진다.
> 일정: 접수 9/18 · 제출 9/20 · 데모데이 10/17 ([MF-011](../../docs/mentor-log.md)). PM 단독 · 팀 로드맵 영향 0.

## 2026-09-07 (일)

### 결정

- **별도 폴더로 빼지 않고 `D/demo/championship-spike` 브랜치에서 계속한다.** 엔진이 `src/kopl` 을 import 하고, 히스토리를
  살려 두면 수상 뒤 `git merge` 로 본 프로젝트에 붙일 수 있다. push·PR 은 여전히 안 한다.
- **배포는 KISIA AWS** (다음 주 발급 예정). 그때까지 로컬에서 완성도만 올린다. 두 프로세스(3000·8000)를 한 인스턴스에 두고
  `PADOPOOL_URL` 만 맞추면 된다.
- **외부 리라이트 LLM 은 Claude API.** OpenAI 키가 없다. `DEMO_EXTERNAL_REWRITE=true` + `ANTHROPIC_API_KEY`, 기본 모델
  `claude-haiku-4-5-20251001`. 배포 때 켜는 것을 추천 — 규칙 후보는 명사 하나짜리(월급·시급·연금)에서 어색하다.
- **규칙 탐지기는 «클래스» 만 고친다.** 개별 표현 추가는 9/28 KoELECTRA 통합 때 버려지는 코드다. 못 잡는 건 괜찮고(화면에
  «규칙 스탑갭» 명시), *잘못 잡는* 것만 지운다.

### 발견 → 본 프로젝트

1. **D05 를 뺀 시딩 인물 4명이 전부 k=1 VERY_HIGH 였다.** 심사위원이 다른 계정을 눌렀으면 바로 들통날 상태.
   - `descendants()` 가 직계 자식만 봐서 **구가 있는 시**(고양·용인·수원·전주·창원·청주·천안·포항·성남·안양)의 하위 읍면동이
     0개 → 인구 0 → `max(1,0)=1`. → **본 프로젝트(C)**: `kopl.c2_specificity` 를 쓰는 쪽에서 시 코드로 하위를 모을 때 같은 함정.
   - **2글자 지명 약칭의 경계 없는 부분 일치.** 코퍼스 2,942편 실측 상위가 전부 오탐 — 한「동안」694 · 「고양」이 156 ·
     휴대「전화」→대전 97 · 「진해」서 61 · 사「진도」38 · 「거창」한 30 · 저「수지」 · 「양산」(우산). → **본 프로젝트(B·C)**:
     사전 매칭 베이스라인(Presidio·korean-pii 비교)에서도 같은 오탐이 난다. 경계 규칙(앞에 한글 금지 · 약칭은 뒤에
     행정 접미사/장소 조사/시설 이름) + 스톱리스트로 잡았다. `detect.py:_place_candidates`.
2. **지명 사전을 표면형마다 `re.finditer` 하면 스캔 한 번에 4~6초.** 3,700개 × 채널 수만큼 컴파일 캐시(512)가 밀린다.
   alternation 하나로 합치니 0.7초. → **본 프로젝트(B)**: 밈·방언·지명 사전 매칭 전부 같은 방식으로.
3. **사전이 같은 이름(중앙동 31곳 · 고성군 2곳)을 처음 것으로 못 박고 있었다.** 「중앙동」만 쓰면 관악구로 귀속. 겹치는 이름은
   정본 대신 이름만 두어 맥락 없이는 깔때기에 안 넣고, 「창원 성산구 중앙동」처럼 상위 지명이 붙어야 확정. → **본 프로젝트(C)**.
4. **연속 지명은 하나의 스팬.** 「김해시 진영읍」이 두 스팬이면 리라이트가 둘로 갈리고 하나만 바꾸면 나머지가 샌다. «앞 지명을
   맥락으로 뒤 지명이 유일하게 풀릴 때만» 합친다 (`kopl.c2_specificity.engine.resolve(name, context=…)` 가 이미 전체 경로를
   받는다 — 단 맥락은 정식 이름이어야 하고 약칭은 사전으로 바꿔 넣어야 한다). → **본 프로젝트(B 라벨 지침·C)**: 라벨 스키마의
   LOC_ADMIN 스팬 경계를 «가장 긴 연속 행정구역» 으로 두는 게 맞는지 확인할 것.
5. **여행·출장 글의 지명은 방문지.** 「제주 3박4일 … 서귀포 … 김포 도착」이 셋 다 거주지 후보가 됐다. 글 단위 표지(N박M일·여행·
   출장·휴가·숙소·공항…)가 있으면 지명을 제외하고 「에 사는·살·집」이 바로 뒤에 붙은 것만 남긴다. 「다녀왔다」는 너무 흔해 뺐다.
   → **본 프로젝트(D 2단)**: 이건 원래 결합 추론 몫. 데모는 근사.
6. 기타 오탐: 「둘째가 **열**이 나서」→나이 10세, 「**둘째**날」→가족, 「손님**이**」 조사 포함. → **본 프로젝트(A 라벨 함정
   목록)**: blind 200 에 이런 함정이 들어 있는지.
7. **🛟(U+1F6DF) 는 Windows 10 에서 □.** 화면은 🌊. → 발표자료·데모 캡처에도 적용.

### 만든 것

- `external.py` Claude API 교체 · `anthropic>=0.30`.
- 우리뜰 2단 레이아웃(≥1024 본문+sticky 레일) · ≤640 하단 탭바 · `urittle.js`(리빌·카운트업·링 게이지·깔때기 로그 막대·
  깔때기↔근거 hover 연결·레일 위젯 `check.json`) · 「이름·전화번호 검사기 vs 파도풀」 비교 스트립 · 조치 후 델타 카운트업.
- **에디터 리라이트 팝오버** — 점검 패널의 색칠된 표현을 누르면 후보 3안 + «이 표현을 빼면 k'»(실제 재계산), 고르면 textarea
  치환 + ✓ + 「다시 점검」 깜빡임. `/api/check` 가 스팬별 `candidates`·`k_without`·`particle` 을 낸다.
- **조사 맞춤** `fit_particle` — 명사형 후보 뒤 이/가·을/를·은/는·과/와·으로/로를 받침에 맞춘다. → **본 프로젝트(D 리라이트
  후처리)** 그대로 쓸 수 있다.
- 실측 (태그 ON/OFF): D05 421/111,069 · D01 36,061/434,408 · D11 1,374/11,651 · D17 244/244 · **D06 5/199,109**.
  D06 이 두 번째 시연감 — 위치태그 하나가 20만 → 5.

### 남은 것

- [ ] KISIA AWS 발급되면 배포 · `DEMO_EXTERNAL_REWRITE` 켜기 · `ANTHROPIC_API_KEY`
- [ ] 시연 대본에 D06 장면 추가 (README 표 참고)
- [ ] 심사위원 문장 훑기 2차 — 리라이트 후보 품질은 Claude 켠 뒤에 다시 본다
- [ ] 법정동(역삼동 등)은 사전에 없어 안 잡힌다 (#115) — 본 프로젝트 C 논점, 데모에서는 안 건드림

## 2026-09-08 (월) 새벽

### 결정 — 리라이트는 «지우기» 가 아니라 «넓히기» 부터, 그리고 «그대로 두기» 도 정식 선택지

「난 김해시 진영읍에 산다」를 「이 근처에 산다」로 바꾸면 필자가 말하려던 것이 사라진다 (사용자 지적). 두 가지로 풀었다.
- **넓히기 사다리** — 지명은 상위 행정구역으로(진영읍 → 김해시 → 경남), 나이는 10년 단위로(쉰셋 → 50대). 단마다 **실제 k**
  (4,949 → 51,140 → 274,310 → 우리 동네 4,178,062). 필자가 「경남 산다」 까지는 말하고 싶으면 그 단을 고른다.
  `recommend.ladder_candidates` + `k_with_note`(스팬 메모만 바꿔 다시 세기) · `specificity.ancestors`.
  → **본 프로젝트(D 조치 추천)**: 리라이트 후보를 «일반화 수준» 으로 설계하면 특정성 k 와 직접 잇는다. 2단 모델 프롬프트에도
  「지우지 말고 한 단계 넓혀라」 를 후보 유형으로 넣을 것.
- **그대로 두기** — 팝오버 하단에 정식 버튼. 고르면 「감수」 표시 + "후보가 N명까지 좁혀지는 건 감수하는 선택" 한 줄, 올리기
  버튼이 「알고도 올리기」. plan.md 의 「조치는 사용자가 고른다」 를 화면이 실제로 지키게 됐다.
- 직업·가족·소득은 사다리가 없어 일반 후보로 간다. 프로필 스팬도 사다리 없음.
- 알려진 것: 기존 글이 이미 더 좁은 지명을 말하고 있으면 사다리가 평평하다(D01 이 김해를 써도 기흥구가 남아 3,136). 맞는 계산.
- **넓혀도 숫자가 안 변하는 이유를 후보 옆에 쓴다.** D01 로 「김해시 진영읍」을 넓히면 김해시·경남 둘 다 3,136 — 다른 글의
  📍신갈동 위치태그가 바닥이기 때문. 사용자가 "왜 둘 다 같지?" 라고 물었고 심사위원도 그럴 것. `k_with_note` 가 그 축의
  최종 단계 근거가 이 스팬이 아니면 `bound`(「다른 글(D01_b02)의 📍기흥구 신갈동 위치태그가 남아 여기까지」)를 낸다.
  깔때기 동률(읍면동 1개 vs 1개)은 초안보다 기존 글을 우선 — 올리기 전/후 비교가 순서에 안 흔들린다.
  → **본 프로젝트(C 기여도·D 추천)**: «어느 근거가 바닥인가» 가 곧 기여도다. 조치 카드에도 같은 설명이 붙어야 한다.
- **바닥이 된 근거를 그 자리에서 치울 수 있게.** 「그것까지 치우면 → 41,478명」 을 같이 세고(`bound_k`), 위치태그면 팝오버의
  「📍 D01_b02 위치태그 끄기」 버튼이 우리뜰 라우트(`POST /posts/<id>/geo_tag`)를 부른 뒤 초안 점검을 자동으로 다시 돌린다.
  표현이면 그 글 수정 화면 링크. 경계는 그대로 — 파도풀은 숫자만 내고, 끄는 건 플랫폼 버튼이다.
  D01 에서 태그를 끄면 다음 바닥은 「기흥호수공원」 4편(기흥구) — 결합 위험이 «한 겹 벗기면 다음 겹» 임을 보여준다.
- **바닥 근거는 «묶음» 이다.** 「기흥」이 4개 글에 있는데 b02 하나만 「치우면 → 41,478(그대로)」 로 나와 사용자가 "아예 바뀌는 게
  없는데?" — 같은 값(정본 지명·나이)을 가리키는 스팬·위치태그를 전부 묶어 「다른 글 4편(b02·b09·b13·b20)의 「기흥」」 으로
  쓰고, 묶음 전체를 뺀 k(김해시 → 51,140 · 경남 → 274,310)를 낸다. 치워도 안 바뀌면(또 다른 바닥) 상자를 안 낸다.
  태그는 글마다 끄기 버튼, 표현은 「내 글 점검에서 4편 ↗」. → **본 프로젝트(C 기여도)**: 기여도는 스팬 단위가 아니라
  «같은 값을 말하는 스팬 묶음» 단위로 세야 한다 — 하나 지워서 안 변하는 기여도는 0 으로 읽히는 함정.
- **심사위원은 인물의 기존 글을 모른다.** 아무 계정에 아무 문장이나 쓰면 기존 글이 만든 바닥(기흥구) 때문에 사다리가 평평해
  "고쳐도 소용없네" 로 읽힌다. ① 글쓰기 기본 작성자를 **🧪 체험 계정(글 0편, `GUEST`)** 으로 — 그 글만으로 k 가 선다
  (51M → 4,949 → 김해시 51,140 → 경남 274,310). 드롭다운의 다른 계정엔 「글 N편 · 이미 드러난 계정」. ② 기존 글이 바닥이면
  패널 위에 「지금 후보 36,061명은 이미 올린 글 20편이 만든 값 — 이 글을 어떻게 고쳐도 그 아래로는 못 넓어집니다 [내 글 점검 ↗]」.
  체험 계정은 이웃 목록에서 뺐다. → **본 프로젝트(E 데모 시나리오)**: 외부 시연에는 «빈 계정» 이 꼭 있어야 한다.

### 심사자 워크플로 — 첫 화면에 «이게 뭐고 뭘 누르라는지» 가 없었다

홈이 그냥 블로그로 보였고, 「내 블로그」가 왜 마당일기인지(로그인 없는 데모 규칙), 핵심 주장(지명 0회 → 421명)이 점검 화면까지
가야 보였다. 형광 스위치는 기본 OFF, 용어(후보·k_level) 설명 없음, 체험 계정 칩이 피드 필터에 노출.
- **홈 첫 방문 카드** — 한 문단 + 3단계 요약 + 「▶ 3분 체험 시작」/「그냥 둘러볼게요」. 시작·종료 상태는 localStorage.
- **게임식 투어 9단계** (`urittle.js` TOUR) — 마당일기 블로그 → 내 글 점검 → 421 요약 → 깔때기 → 위치태그 끄기(클릭) →
  421→111,069 → 글쓰기 → 예문 자동 입력·체험 계정 선택·점검 → 색칠된 표현. 대상에 링 하이라이트 + 말풍선, 클릭이 필요한
  단계는 그 버튼을 누르면 자동 진행, 페이지가 바뀌어도 이어진다. 끝나면 레일에 「다시 보기」만.
- 레일 맨 위 「로그인 없는 데모 — 보고 있는 블로그의 주인이 «나»」 · 점검 화면 형광 기본 ON · 후보/k_level/위험 점수 툴팁 ·
  체험 계정 칩 숨김.
→ **본 프로젝트(E W10 시연 영상·A 발표)**: 발표 대본이 곧 이 9단계다. 영상도 같은 순서로 찍으면 된다.

### 이건 완성이 아니다 — 통합 계획을 README 에 못 박았다

9/18 링크 기준선. 이후 A 코퍼스 v1(W4) · E 시딩(W4~5) · **B KoELECTRA(W5~7, `C1_MODEL_PATH`)** · C 기여도(W5) · E 메타 관리(W6) ·
**D Qwen3 2단(W7)** · E 에디터 경고(W9) 순으로 스탑갭을 뺀다. 표는 README 「통합 계획」. 주 1회 `git merge main` →
`probe.py --diff`(5명 k 회귀, `probe.baseline.json` 이 정본) → 바뀐 숫자를 README·LOG 에.
주의: B 모델은 스팬만 낸다 — 합치기·여행·시제·타인 귀속 후처리는 모델 경로에도 붙여야 한다 (지금은 규칙 경로에만 있다).

- **D17 오수양반 제외.** 본문에 「오수면」이 그대로 나와 위치태그 ON/OFF 가 244 로 같다 — 조치 ② 를 못 보여준다. 시딩·프로브·README 에서 뺐다. 이웃 4명.

## 2026-09-08 (월)

- **시연 인물 교체 — D11 옥과할매·D17 오수양반 → E20 종강했음(21세 여대생·세종)·C02 주차먼저(46세 남·배송기사·대전).**
  110명 전부 돌려서(태그 ON k 2~2,000 · 깔때기 3단 · 태그 효과 3배 · 오탐 적음) 고르고 글을 읽어 확인. D11·D17 은 시골
  노년이 D05 와 겹치고 태그 효과가 약했다. 이제 시골 노년(D05)·30대 연구직(D06)·40대 직장인(D01)·20대 대학생(E20)·
  40대 배송기사(C02). 정본 수치 갱신: E20 1,231/17,433 · C02 899/14,574. 참고로 이제 **k=1 인 인물이 0명** (어제 버그 수정 효과).
- **성별 규칙 오탐 2부류** — 「아짐」이 좋**아짐**·나**아짐** 에, 「아내」가 찾**아내**·잡**아내** 에 걸려 남성(D12·E02·C19)이
  「여성」으로, 여성(A05)이 「남성」으로 좁혀졌다. 앞뒤 한글 금지로 고쳤다. 4명 baseline 변화 없음.
  → **본 프로젝트(A 라벨 함정·B)**: 인터넷 말투 명사형 어미(-아짐·-아내·-아짐) 가 가족 어휘와 겹친다 — blind 200 에 넣을 것.
- **홈에 「나」가 없다.** `me` 가 마당일기로 기본값 처리돼 홈 레일에 마당일기 프로필·「나」 배지가 붙어 심사자가 "이 블로그의 나는 마당일기인가?" — 홈·글쓰기에서는 `me=None`, 레일 문구 "이웃을 고르면 그 계정이 «나»". 헤더·탭바에 **「⟲ 처음부터」** — 첫 화면 + 투어 1단계부터 (투어 중에도 첫 방문 카드는 남긴다).
- **댓글 256개를 직접 썼다.** 글 ID 로 뽑던 문구 8개가 내용·말투와 안 맞았다(사용자 지적). 인물마다 이웃 집단을 정하고(시골 어르신·손녀 / 낚시 이웃 / 대학 친구 반말 / 동료 기사 격식체 / 차분한 존댓말 독자) 글 내용에 반응하게, 글에 없는 신상은 안 넣게. `data/comments.json` · 탐지기로 훑어 새 지명·나이 없음 확인. 파일이 있으면 심사자 새 글은 댓글 0. `gen_comments.py` 는 인물 교체 때 초안용.

## 2026-09-12 (금)

### 우리뜰 → Veilo (베일로) — 이름·디자인 교체

- **경위**: 멘토가 「우리뜰은 아무 의미도 없고 유치하다」. 후보를 한국어 단어 → 영어 단어 → 합성어(Storylog 등)로 세 번 냈고, 사용자 기준은
  「런칭할 게 아니니 심사위원이 듣자마자 SNS 구나 하면 된다」. 팀원들이 각자 이미지 시안(Letterly·Knocky·Veilo)을 냈고 투표로 **Veilo** 확정.
  디자이너 핸드오프 v1.1(docx)까지 같이 왔다 → `design/veilo-handoff-v1.1.md` 로 텍스트만 저장.
- **결정**: 파도풀 이름은 유지(플랫폼 ≠ 점검 서비스 논지). 적용은 리스킨이 아니라 **핸드오프 레이아웃까지** — 좌측 사이드바(프로필·메뉴·파도풀 위젯·이웃),
  상단 검색·글쓰기·알림·프로필, 브랜드 헤더(심볼 + Veilo + 메시지, 추상 도형), 카테고리 탭(홈·글·사진·영상·방명록 — 영상·방명록은 데모 빈 상태), 모바일은 ☰ 드로어.
  토큰·폰트(Pretendard·Plus Jakarta Sans)·라운드 16·버튼 40~44·인풋 44~48 을 따랐다. 파도풀 패널은 바다 톤을 남기고 바닥만 아이보리로.
- 투어·탭·위젯·리셋은 셀렉터를 유지해 그대로 돈다(`.rail-nb a[href=…]`, `.ut-tabs a[href='/new']`). 정적 파일은 `?v=<mtime>` 로 캐시 무효화.
- 파일 이름 `urittle.*` → `veilo.*`. 클래스 접두사 `ut-` 는 그대로(템플릿·스크립트가 같이 씀). 저장소 안 「우리뜰」 표기는 LOG 의 과거 기록만 남긴다.
- → **본 프로젝트(E 시스템)**: E 의 W5 웹앱이 이 토큰·레이아웃을 그대로 쓰면 된다 — `design/` 폴더가 기준.

### 사용자 피드백 5건 (Veilo 적용 직후)

| # | 피드백 | 한 것 |
|---|---|---|
| 1 | 리라이트 팝오버 줄이 꼬이고 hover 범위가 이상 | Veilo 의 전역 `button{height:40px;display:inline-flex}` 이 팝오버 줄(grid)을 눌러 놓은 것. 팝오버 안에서 되돌림 |
| 2 | 표현을 고치면 투어 말풍선이 고친 자리를 가림 | ⑩ 단계는 말풍선을 **위**에 붙이고(`place:"above"`), 고친 뒤 말풍선을 다시 놓는다. 고친 표현(.fixed)도 타깃으로 |
| 3 | 홈 카드에서 사진이 있는지·무엇인지 모름 | 카드에 사진 띠(작은 액자 + 「사진 설명」 캡션, 3장까지 + N) |
| 4 | 새로고침하면 처음부터 시작돼야 | F5(reload)면 투어 상태를 지우고 홈으로. 글 데이터는 그대로 |
| 5 | 스포트라이트가 스크롤에 뒤늦게 따라오고 켜진 채 남음 | 스크롤 중엔 전환 없이 즉시 따라가고, 켜진 시간(1.1초)은 늘리지 않는다 |

## 2026-09-11 (목)

### 성능 — 「내 글 점검」 4.8초 → 0.7초. 원인은 엔진이 아니라 「localhost」

- 사용자가 「최적화 잘했어?」 라고 물어 재 봤다. 정적 CSS 한 장도 2초, 점검 화면 4.8초. 엔진은 analyze 0.07 · compute 0.20 · recommend 0.33초뿐.
- **원인**: Windows 에서 `localhost` 는 IPv6(::1) 를 먼저 시도해 실패한 뒤 IPv4 로 간다 — 요청마다 약 2초. 우리뜰이 파도풀을 `http://localhost:8000` 으로
  부르니 점검 한 번에 2초, 레일 위젯(check.json)에 또 2초, 작성자별 비교는 작성자마다 2초씩. **기본값을 `127.0.0.1` 로.** 배포(Linux)에선 안 나는 문제라
  로컬 시연·터널에서만 체감되던 것. → **본 프로젝트(E 시스템)**: 서비스끼리 부를 땐 localhost 대신 127.0.0.1 또는 컨테이너 이름.
- 덤: 응답 gzip(첫 화면 191KB → 52KB · CSS·JS 74KB → 21KB), 정적 파일 1시간 캐시. 폰트는 이미 7면 → 4면(9/10).
- 아직 안 한 것: Flask 개발 서버(단일 스레드) 그대로 — AWS 에선 waitress/gunicorn. 웹폰트 자체를 뺄지는 이름·디자인 확정 뒤 결정.

## 2026-09-11 (목) 새벽

### 역 사전 — 「용마산역」 이 잡히고 후보 수까지 줄어든다 (E 피드백 ①, 밤새 작업)

- **결정**: 억지 규칙(「○○역」 접미사만 색칠)이 아니라 **역 → 행정동 사전**으로 간다. C 에게 부탁하지 않고 PM 이 만들되, 특정성 규칙의 판단
  (역 하나를 동 하나로 볼지 이웃까지 합칠지)은 **스위치로 남겨** C 가 고르게 한다. 사용자 결정 9/10 밤.
- **자료**: ① 공공데이터포털 「전국도시철도역사정보표준데이터」 898건(좌표·노선·주소, 기준일 2019~2021) — 키 없이 `columList.json → standard.json` 으로 받았다.
  ② 행정동 경계 GeoJSON(vuski/admdongkor, 행안부 adm_cd2, 2026-07 판). 둘 다 공개 자료.
- **방법**: 점-다각형 판정(순수 파이썬, 1.7초)으로 역이 든 행정동 `emd`, 반경 700m 안에 경계가 닿는 `near`. 같은 이름의 역은 1.5km 안이면 환승역 하나로
  합치고(서울역 1·4호선), 멀면(서울 시청역·부산 시청역·대전 시청역) `ambiguous` — 맥락 없이 못 정하니 세지 않는다(20개 이름). 764개 역, 코드 미해결 0.
  경계 코드가 사전(regions.json)에 전부 있었다 — 같은 행안부 코드라서.
- **탐지 규칙(클래스)**: 「○○역」 꼴은 언제나, 역을 뗀 꼴(태릉입구)은 3자 이상이고 뒤에 장소 조사·앞·근처·입구 가 올 때만. 앞에 한글이 붙으면(서「울역」) 아니다.
  타인·과거·이동 배제는 지명과 같은 규칙을 탄다.
- **결과**: 「용마산역 근처에 산다. 서른셋이다」 → 5,100만 → 면목제4동 17,439 → 30~34세 1,372. `near` 면 79,803 → 7,175. 사다리: 역 → 중랑구 33,187 → 서울 810,079.
- **probe 가 1건 바뀌었다 — C02 태그 OFF 14,574 → 899.** 「노은역 인근 상가」 3편이 노은1동으로 풀려 태그 없이도 같은 동에 닿는다. 확인해 보니 인물
  설계(design_note)가 정확히 그렇게 심어 둔 단서였다 — 「노은역 방향 이동과 방문 분포를 결합해 노은1동 생활권이 드러나도록」. 오탐이 아니라 설계 의도를 찾은 것.
  baseline 갱신. C02 는 이제 태그 끄기 효과가 0 이라 시연은 D05·D06 으로.
- **→ 본 프로젝트(C)**: `data/stations.json` + 빌더를 그대로 가져갈 수 있다. C 가 정할 것 하나 — `emd` 냐 `near` 냐. **(B)**: 모델이 「노은역」을 LOC_FACILITY 로
  잡으면 그 뒤는 이 사전이 푼다. 골드에 LOC_FACILITY 「신갈저수지」 같은 스팬이 이미 있다.
- **→ 본 프로젝트(A·D 라벨 지침)**: 「역 인근 상가 주소」 처럼 *방문지* 로 적은 역도 반복되면 생활권 단서다. 이건 여행·출장 배제와 다르다 — C02 가 그 증거.
- 28 passed. 스크립트 `tools/build_stations.py`, 스위치 `DEMO_STATION_SCOPE`.

## 2026-09-10 (수)

- **멘토가 직접 만져 볼 수 있게 밖으로 냈다 — 무료 터널 + 접속 열쇠.** GitHub Pages 는 정적이라 Flask 두 개(:3000·:8000)와 SQLite 를 못 올린다(사용자 질문). AWS 는 아직이라 cloudflared 임시 터널(계정·요금 없음)로 :3000 만 냈고, `DEMO_ACCESS_KEY` 를 두어 `?key=` 로 들어온 사람에게만 쿠키를 주고 나머지는 403. 열쇠는 저장소 밖(`%TEMP%\padopool_key.txt`). 밖에서 확인: 열쇠 없음 403 · 열쇠 있음 200+쿠키 · 정적·점검 페이지 200. 절차는 `HANDOFF.md` 실행 절. → **본 프로젝트**: AWS 배포 때도 같은 열쇠 게이트를 그대로 쓰면 접수 링크를 심사 전에 아무나 못 연다.
- 터널을 내리면 주소가 사라진다. 멘토 세션이 끝나면 `seed.py --reset` 으로 되돌릴 것.

### 팀원·멘토 피드백 20건 (9/10 저녁, 직접 써 보고) → 전부 반영

| # | 피드백 | 한 것 |
|---|---|---|
| 1 | 「처음부터」가 글 데이터까지 되돌려야 | `POST /demo/reset` — 같은 프로세스에서 seed 를 다시 돌리고 세션도 비운다. 헤더·탭바 버튼이 여기로 |
| 2 | 로그인·회원가입이 형식적으로라도 | `/login` `/signup` 화면. 인증 없음, 누르면 홈. 헤더에 「로그인」 |
| 3 | 사진이 사진 같지 않고 캡션인지 모르겠다 | 글 화면: 16:9 액자 + 「사진 N / M」 + 「사진 설명」 라벨 캡션. 근거 카드: 작은 액자 + 「사진 설명」 칩 |
| 4 | ?·등급·점수는 올리면 바로 설명이 떠야 | `title` 툴팁 → CSS 즉시 팝오버(`.tip`), 올리면 색이 바뀌고 키보드 포커스로도 뜬다. 「k_level」 → 「후보 등급」 |
| 5 | 글 위 사투리 칩이 왜 이 색인지 | 칩에 🗣 + 툴팁, 범례에 「🗣 말투로 잡은 지역」, 안내문에 한 줄 |
| 6 | `.ut-pado::after` 장식이 버튼을 가림 | `pointer-events:none` |
| 7 | 근거 문장 카드가 많으면 스크롤 | 3편만 보이고 「나머지 N편 펼치기」, 카드 머리를 누르면 접힘, 모두 접기/펼치기. 깔때기에서 오면 자동으로 펼친다 |
| 8 | 한 번 고친 표현을 다시 누를 수 있게 | 고친 표현(✓)을 누르면 후보가 다시 뜨고 「↩ 원래 표현으로 되돌리기」 도 있다. 지금 들어 있는 글자를 기준으로 치환 |
| 9 | 조치 번호가 ①②③ 순서가 아님 | 종류별 번호를 버리고 위에서부터 ①②③ (레일 체크리스트도) |
| 10 | 표현 n개 × 3안 = 3n 줄 | 후보를 칩 한 줄로, 고른 것만 문장·유사도·버튼이 펼쳐진다 |
| 11 | 점검 화면 정보량 — 페이지를 나누자 | **네 걸음 탭**(요약 → 어떻게 좁혀지나 → 근거 문장 → 이렇게 하면 넓어져요). 카드마다 이전/다음, 해시로 바로 가기, 레일 목차·깔때기 클릭·투어가 탭을 자동으로 켠다 |
| 12 | 문장 전체가 아니라 「예순여덟」만 빨간 줄 | `span_text` 를 API 에 실어 그 부분만 `<del>` |
| 13 | 비공개할 글이 어떤 글인지, 어떤 단서가 사라지는지 | 조치 카드에 제목·발췌·「이 글을 내리면 사라지는 단서」 칩(엔진 `_cut`). 위치태그 카드도 글 제목·태그 |
| 14 | 마당일기 제목이 전부 「구월 이튿날」, 날짜와도 안 맞음 | 코퍼스는 동결이라 `data/titles.json` 으로 데모 시딩에서만 덮어쓴다(단서 없는 제목 17개). probe 「변화 없음」 |
| 15 | 폰트를 너무 많이 받아옴 | 7면 → 4면 (Sans KR 400·700, Mono 500, 고운돋움) |
| 16 | 「강남에 산다」는 안 잡히고 「경남에 산다」는 잡힘 | 스톱리스트 약칭(강남·서초·화성…)을 **거주 서술이 바로 뒤에 올 때만** 지명으로 — `guarded_lexicon` + `_LIVES_AFTER`(산다·산 지·삽니다 추가). → **본 프로젝트(B·C)**: 일반명사와 겹치는 약칭은 «버리기» 가 아니라 «문맥 조건부» 가 맞다 |
| 17 | 「감수하는 선택이에요」 가 공격적 | 「…까지 줄어들 수 있어요」 |
| 18 | 숫자가 큰 게 좋은 건지 모르겠다 | 에디터 결과 밑에 「숫자는 나일 수 있는 사람 수. 클수록 안전, 작을수록 특정되기 쉬움」 |
| 19 | 첫 모달 가독성 · 1-2-3 이 서비스 사용 순서였으면 | 줄 길이 46em, 「이렇게 써요」 ① 평소처럼 글 ② 올리기 전 버튼 하나 ③ 고르고 올리기. 투어 순서는 한 줄로 |
| 20 | 같은 글이라도 작성자에 따라 위험도가 다르다는 단계 | 에디터에 「같은 글을 다른 사람이 올리면?」 — `/check-draft.json` 을 작성자마다 불러 표(지금 / 이 글을 올리면 / 등급). 투어 ⑪⑫ 로 추가, 총 12단계(근거 문장 단계 ⑤ 도 추가) |

- 덤으로 잡은 것: 시딩을 되돌린 뒤에도 쿠키에 옛 k 가 남아 「조치를 반영했어요」 배너가 뜨고 투어가 6단계로 튀던 것 — 조치 라우트를 지난 직후(`session["acted"]`)에만 배너를 낸다. 터널 리허설에서 4→7 로 튀던 원인(태그가 이미 꺼진 DB)도 ①로 해결.
- 25 passed · probe 「변화 없음」.

### E 피드백 (9/10 저녁, GUIDE 대로 직접 돌려 봄)

1. **역명(용마산역·태릉입구) 미탐지** — 맞다. 데모 사전은 kopl 행정구역뿐이고 시설 이름은 아는 지명 뒤에 붙을 때만 잡는다. 저장소에 역·시설 사전이 없다.
   제대로 된 답은 B 모델(골드에 LOC_FACILITY 「신갈저수지」 등이 있다). 역→읍면동 매핑이 있어야 후보 수까지 줄일 수 있는데 그건 C 의 시설 사전 논점.
   → **본 프로젝트(B·C)**. 데모에서는 보류 — 다음 주 C 의 W5 계획을 물어본 뒤 정한다(억지 규칙보다 매핑이 낫다는 PM 판단).
2. **넓히기 뒤 재수정 불가 · 「우리 근처 근처에서」** — 재수정은 같은 날 반영분(고친 표현 다시 누르기·되돌리기)과 겹친다. 중복은 진짜 버그:
   붙어 있는 두 표현을 각각 넓혀 같은 말로 끝나고 시작하면(「근처」+「근처」, 「우리 근처」+「근처」) 뒤엣것을 지운다(`mergeDup`). 미리보기가
   읽기 전용이라는 걸 몰라도 되게 — 미리보기 글자를 누르면 본문 칸의 그 자리로 커서가 가고, 「✎ 본문에서 직접 고치기」 버튼과 팝오버의
   「내가 직접 고칠게요」 줄을 뒀다. → **본 프로젝트(E W9)**: 자동 치환은 인접 스팬을 한 단위로 봐야 한다.

## 2026-09-09 (화)

- **E 합류.** E 가 본 프로젝트 빈 시간에 대회를 같이 준비하겠다고 해서 데모 브랜치를 push 해 공유한다. 살펴보고 배우고 고칠 점이 보이면 고치는 식. 채택은 E 판단, 본 프로젝트 우선. `GUIDE.md`(띄우기·볼 것·코드 지도·깨지면 안 되는 것·시작하기 좋은 일·작업 방식) 를 썼다. main PR 은 여전히 안 한다. 멘토에게는 따로 알리지 않는다(사용자 결정).
- **화면 문구 전면 손질.** 「형광 자리가 새는 표현」 같은 개발 은유·용어(스팬·채널·2단 몫·k_level 설명·body/geo_tag)가 화면에 올라와 있었다(사용자 지적). 해요체로 통일, 대시(—) 51→0(제목 제외)·중점(·) 74→33, 용어 줄 9→0. 엔진의 condition·why·rationale 문자열도 함께. im-not-ai(humanize-korean) 규칙집을 체크리스트로 씀 — 채점기는 긴 글용이라 마이크로카피엔 「위험 낮음」이 나와 참고만. 22 passed · k 변화 없음.
- **첫 방문은 모달, 스포트라이트는 1초.** 시작 버튼을 눌러도 화면이 그대로라 눌린 줄 몰랐고(사용자), 어둡게 계속 두면 다른 데를 안 둘러본다(사용자). 첫 방문 = 가운데 모달(소개 + 시작/둘러보기, 바깥 클릭·Esc 는 둘러보기), 시작하면 모달이 닫히고 상단에 진행바(N/9·그만하기), 단계마다 1초만 어둡게 뚫었다가 걷힘. 「⟲ 처음부터」는 모달로 돌아온다.
